// import type { HotelPartner } from '../types';
import type { HotelPartner } from "../../hotelPartners/types";

export const fetchPartners = async (): Promise<HotelPartner[]> => {
  return [];
};

export const updatePartnerPermissions = async (_id: number, _permissions: any): Promise<void> => {};